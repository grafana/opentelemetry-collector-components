gcomapi:
  client:
    endpoint: "http://localhost:3000"
    key: "test_key"
    timeout: 1s
  cache:
    complete_refresh_duration: 1s
    incremental_refresh_duration: 1s
  grafana_cluster_filters: 1,2

gcomapi/missing_endpoint:
  client:
    endpoint: ""
    key: "test key"

gcomapi/missing_key:
  client:

gcomapi/missing_grafana_cluster_filters:
  client:
    endpoint: "http://localhost:3000"
    key: "test key"
